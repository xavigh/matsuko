{% extends 'base.html.twig' %}
{% form_theme form 'form/fieldForm.html.twig' %}

{# title #}
{% block title %}Form categories {% endblock %}
{#  Page Content #}
{% block body %}
{# app/Resources/views/default/new.html.twig #}
<div class="container mb-3">
    <h1 class="mt-4 mb-3">Add New Category</h1>
    <div class="row ">
        <div class="col-md-8">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
